import unittest
from flask import Flask
from app.controllers.{{ entity.name.lower() }}_controller import {{ entity.name.lower() }}_bp

class Test{{ entity.name }}Controller(unittest.TestCase):
    def setUp(self):
        self.app = Flask(__name__)
        self.app.register_blueprint({{ entity.name.lower() }}_bp, url_prefix='/api/{{ entity.name.lower() }}s')
        self.client = self.app.test_client()

    def test_get_all_{{ entity.name.lower() }}s(self):
        response = self.client.get('/api/{{ entity.name.lower() }}s')
        self.assertEqual(response.status_code, 200)
        # Add more assertions as needed

    def test_create_{{ entity.name.lower() }}(self):
        response = self.client.post('/api/{{ entity.name.lower() }}s', json={})
        self.assertEqual(response.status_code, 201)
        # Add more assertions as needed
